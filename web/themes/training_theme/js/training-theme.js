/**
 * @file
 * Training theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.trainingTheme = {
    attach (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
